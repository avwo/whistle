var winHostsPath = 'C:/Windows/system32/drivers/etc/hosts';
var macHostsPath = '/private/etc/hosts';
var linuxHostsPath = '/etc/hosts';

function getHosts() {
	
}

function setHosts(hosts) {
	
}

function getProxy() {
	
}

function setProxy(ip, port) {
	
}

function resetProxy() {
	
}

module.exports = {
		getHosts: getHosts,
		setHosts: setHosts,
		getProxy: getProxy,
		setProxy: setProxy,
		resetProxy: resetProxy
};

